<h1>New Station</h1>

<%= render 'form', station: @station %>

<%= link_to 'Back', stations_path %>

<%# put in _station.json.jbuilder: json.extract! location, :id, :address, :latitude, :longitude, :created_at, :updated_at
json.url location_url(location, format: :json) %>